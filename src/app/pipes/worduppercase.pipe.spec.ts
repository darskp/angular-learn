import { WorduppercasePipe } from './worduppercase.pipe';

describe('WorduppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new WorduppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
